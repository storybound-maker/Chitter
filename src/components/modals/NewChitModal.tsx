import React,{useState,useEffect,useRef}from'react';import{X,Sparkles,Trash2,Image as ImageIcon,Video}from'lucide-react';import{useChitter}from'../../context/ChitterContext';
export const NewChitModal:React.FC<{initialImageUrl?:string}>=({initialImageUrl})=>{const{isNewChitModalOpen,setNewChitModalOpen,addRealmPost}=useChitter();const[c,setC]=useState(''),[tags,setTags]=useState('#Chitter #Realm'),[media,setMedia]=useState('');const file=useRef<HTMLInputElement>(null);useEffect(()=>{if(initialImageUrl)setMedia(initialImageUrl)},[initialImageUrl,isNewChitModalOpen]);if(!isNewChitModalOpen)return null;const pick=(f:File|null)=>{if(!f)return;setMedia(f.type.startsWith('video/')?'video:'+URL.createObjectURL(f):URL.createObjectURL(f))};const post=()=>{if(!c.trim()&&!media)return;addRealmPost(c.trim()||'Shared a moment in Chitter Realm ✨',tags.split(' ').filter(x=>x.startsWith('#')),media||undefined);setC('');setMedia('');setNewChitModalOpen(false)};const video=media.startsWith('video:');const src=video?media.slice(6):media;return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 p-4 backdrop-blur-md"><div className="w-full max-w-md rounded-3xl border border-zinc-800 bg-zinc-950 p-5"><div className="flex items-center justify-between border-b border-zinc-900 pb-4"><div className="flex items-center gap-2"><Sparkles className="h-5 w-5 text-cyan-400"/><h2 className="font-bold">Create New Chit</h2></div><button onClick={()=>setNewChitModalOpen(false)}><X/></button></div><textarea value={c} onChange={e=>setC(e.target.value)} rows={3} placeholder="What is happening in your realm?" className="my-4 w-full rounded-2xl border border-zinc-800 bg-zinc-900 p-4 text-sm outline-none"/>{media&&<div className="relative mb-4 overflow-hidden rounded-2xl border border-zinc-800 bg-black">{video?<video src={src} controls className="max-h-80 w-full object-contain"/>:<img src={src} className="max-h-80 w-full object-contain"/>}<button onClick={()=>setMedia('')} className="absolute right-2 top-2 rounded-full bg-black/80 p-2 text-rose-400"><Trash2 className="h-4 w-4"/></button></div>}<div className="flex gap-2"><button type="button" onClick={()=>file.current?.click()} className="flex items-center gap-2 rounded-full border border-zinc-800 px-4 py-2 text-xs font-bold"><ImageIcon className="h-4 w-4 text-cyan-400"/>Picture</button><button type="button" onClick={()=>file.current?.click()} className="flex items-center gap-2 rounded-full border border-zinc-800 px-4 py-2 text-xs font-bold"><Video className="h-4 w-4 text-cyan-400"/>Media</button><input ref={file} hidden type="file" accept="image/*,video/*" onChange={e=>pick(e.target.files?.[0]||null)}/></div><input value={tags} onChange={e=>setTags(e.target.value)} className="mt-4 w-full rounded-2xl border border-zinc-800 bg-zinc-900 px-4 py-2 text-xs text-cyan-400 outline-none"/><div className="flex justify-end gap-2 pt-4"><button onClick={()=>setNewChitModalOpen(false)} className="px-4 py-2 text-sm text-zinc-400">Cancel</button><button onClick={post} disabled={!c.trim()&&!media} className="rounded-full bg-cyan-400 px-5 py-2 text-sm font-bold text-black disabled:opacity-40">Post Chit</button></div></div></div>};
