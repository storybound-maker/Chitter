import React,{useState}from'react';import{Search,Archive,MessageSquare}from'lucide-react';import{useChitter}from'../../context/ChitterContext';import{ChatListItem}from'./ChatListItem';import{PacmanAvatar}from'../common/PacmanAvatar';
const STORIES=['Ava Collins','Ethan','Zoe Carter','Mila Harper','Santiago'];
export const ChatterView:React.FC<{onOpenCamera:()=>void}>=({onOpenCamera})=>{const{chatItems,openChat,searchQuery,setSearchQuery}=useChitter();const[filter,setFilter]=useState<'all'|'unread'|'groups'>('all');const chats=chatItems.filter(c=>(c.name.toLowerCase().includes(searchQuery.toLowerCase())||c.lastMessage.toLowerCase().includes(searchQuery.toLowerCase()))&&(filter==='all'||filter==='unread'&&c.unreadCount>0||filter==='groups'&&c.isGroup));return <div className="relative min-h-full pb-28 text-white"><div className="sticky top-0 z-20 bg-zinc-950/95 px-4 pb-3 pt-3 backdrop-blur-xl"><div className="mb-3 flex items-center justify-between"><div className="flex items-center gap-2"><div className="flex h-8 w-8 items-center justify-center rounded-full border border-cyan-400/40 bg-cyan-950 text-cyan-400"><MessageSquare className="h-4 w-4"/></div><h2 className="text-xl font-black">Chatter</h2></div><button className="flex items-center gap-1 rounded-full border border-zinc-800 bg-zinc-900 px-3 py-1 text-xs text-zinc-400"><Archive className="h-3.5 w-3.5"/>Archive</button></div><div className="relative mb-3"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-cyan-400"/><input value={searchQuery} onChange={e=>setSearchQuery(e.target.value)} placeholder="Search chatter" className="w-full rounded-full border border-zinc-800 bg-zinc-900 py-2.5 pl-9 pr-4 text-xs outline-none focus:border-cyan-400"/></div><div className="flex gap-3 overflow-x-auto no-scrollbar">{STORIES.map(name=><div key={name} onClick={onOpenCamera} className="flex shrink-0 flex-col items-center gap-1"><PacmanAvatar size={48} hasStory/><span className="max-w-14 truncate text-[10px] text-zinc-400">{name}</span></div>)}</div></div><div className="flex gap-2 border-b border-zinc-900 px-4 py-2 text-xs">{(['all','unread','groups']as const).map(f=><button key={f} onClick={()=>setFilter(f)} className={`rounded-full px-3 py-1 font-bold ${filter===f?'bg-cyan-400 text-black':'bg-zinc-900 text-zinc-400'}`}>{f[0].toUpperCase()+f.slice(1)}</button>)}</div><div>{chats.map(c=><ChatListItem key={c.id} chat={c} onClick={()=>openChat(c.id)}/>)}</div></div>};
